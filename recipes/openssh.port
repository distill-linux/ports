PORT_NAME="openssh"
PORT_VERSION="10.5p1"
PORT_RELEASE="1"
PORT_DESC="OpenSSH free Secure Shell (SSH) client and server implementation"
PORT_URL="https://www.openssh.com"
PORT_COMMIT="10.5p1"
BUILD_SYSTEM="gnu-configure"
BUILD_DEPS="automake"
RUN_DEPS="zlib"

BUILD:
./configure --prefix=/usr CC=clang
make -j$(nproc)
make DESTDIR="$PKG_FAKEROOT" install
